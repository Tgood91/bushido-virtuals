import React, { useState, useEffect } from 'react';
import { TrendingUp, Zap, Target, AlertCircle, CheckCircle, ChevronRight } from 'lucide-react';

const BushidoTradingDashboard = () => {
  const [selectedStrategy, setSelectedStrategy] = useState('seykota');
  const [virtueScores, setVirtueScores] = useState({
    gi: 0.92,
    yu: 0.78,
    jin: 0.85,
    rei: 0.88,
    makoto: 0.95,
    meiyo: 0.81,
    chugi: 0.89
  });

  const virtues = [
    { id: 'gi', name: 'Gi', meaning: 'Righteousness', principle: 'Signal Integrity' },
    { id: 'yu', name: 'Yu', meaning: 'Courage', principle: 'Risk Discipline' },
    { id: 'jin', name: 'Jin', meaning: 'Benevolence', principle: 'Liquidity Respect' },
    { id: 'rei', name: 'Rei', meaning: 'Respect', principle: 'Fee Awareness' },
    { id: 'makoto', name: 'Makoto', meaning: 'Honesty', principle: 'Transparency' },
    { id: 'meiyo', name: 'Meiyo', meaning: 'Honor', principle: 'Performance Integrity' },
    { id: 'chugi', name: 'Chugi', meaning: 'Loyalty', principle: 'Framework Consistency' }
  ];

  const strategies = [
    {
      id: 'seykota',
      name: 'Seykota',
      theme: 'Ed Seykota - The Trend Follower',
      status: 'active',
      lastSignal: 'BUY AERO/USDC on EMA cross',
      confidence: 0.87,
      positionSize: '15.3%',
      virtueWeight: { gi: 0.95, yu: 0.90, jin: 0.80, rei: 0.92, makoto: 0.98, meiyo: 0.85, chugi: 0.92 }
    },
    {
      id: 'druckenmiller',
      name: 'Druckenmiller',
      theme: 'Stanley Druckenmiller - The Macro Master',
      status: 'monitoring',
      lastSignal: 'HOLD - Macro regime uncertain',
      confidence: 0.62,
      positionSize: '8.7%',
      virtueWeight: { gi: 0.88, yu: 0.65, jin: 0.90, rei: 0.84, makoto: 0.92, meiyo: 0.78, chugi: 0.86 }
    }
  ];

  const auditLog = [
    { time: '14:23 UTC', action: 'EXECUTE SWAP', asset: 'DEGEN→USDC', amount: '2,450', virtue: 'makoto', status: 'success' },
    { time: '14:15 UTC', action: 'SIGNAL GENERATED', agent: 'Seykota', signal: 'BUY AERO', virtue: 'gi', status: 'pending' },
    { time: '14:08 UTC', action: 'SLIPPAGE CHECK', pool: 'HIGHER/USDC', slippage: '0.43%', virtue: 'jin', status: 'success' },
    { time: '13:52 UTC', action: 'EXECUTE SWAP', asset: 'PRIME→VIRTUAL', amount: '1.2K', virtue: 'meiyo', status: 'success' }
  ];

  const overallScore = (Object.values(virtueScores).reduce((a, b) => a + b) / Object.keys(virtueScores).length * 100).toFixed(1);

  const getScoreColor = (score) => {
    if (score >= 0.9) return 'from-green-500 to-emerald-500';
    if (score >= 0.8) return 'from-blue-500 to-cyan-500';
    if (score >= 0.7) return 'from-yellow-500 to-orange-500';
    return 'from-red-500 to-orange-500';
  };

  return (
    <div className="min-h-screen bg-gradient-to-br from-slate-950 via-slate-900 to-slate-800 text-gray-100 p-6">
      {/* Header */}
      <div className="mb-8 border-b border-slate-700 pb-6">
        <div className="flex items-center justify-between mb-4">
          <div>
            <h1 className="text-4xl font-bold text-transparent bg-clip-text bg-gradient-to-r from-amber-400 to-orange-500 mb-2">
              ⛩ The Way of Trade
            </h1>
            <p className="text-slate-400 text-sm">Bushido Trading Protocol • Developer Dashboard</p>
          </div>
          <div className="text-right">
            <div className="text-5xl font-bold text-amber-400">{overallScore}%</div>
            <p className="text-slate-400 text-xs">Virtue Alignment</p>
          </div>
        </div>
      </div>

      <div className="grid grid-cols-1 lg:grid-cols-4 gap-6 mb-8">
        {/* Virtue Hexagon */}
        <div className="lg:col-span-1">
          <div className="bg-slate-800/50 border border-slate-700 rounded-lg p-6 backdrop-blur">
            <h3 className="text-amber-400 font-bold text-sm mb-4">VIRTUE PATH</h3>
            <div className="space-y-3">
              {virtues.map((virtue) => (
                <div key={virtue.id} className="space-y-1">
                  <div className="flex justify-between items-center text-xs">
                    <span className="text-slate-300">{virtue.name}</span>
                    <span className="text-amber-400 font-mono">{(virtueScores[virtue.id] * 100).toFixed(0)}%</span>
                  </div>
                  <div className="w-full bg-slate-700 rounded-full h-1.5 overflow-hidden">
                    <div 
                      className={`h-full bg-gradient-to-r ${getScoreColor(virtueScores[virtue.id])}`}
                      style={{ width: `${virtueScores[virtue.id] * 100}%` }}
                    />
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>

        {/* Active Strategies */}
        <div className="lg:col-span-3">
          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
            {strategies.map((strategy) => (
              <div
                key={strategy.id}
                onClick={() => setSelectedStrategy(strategy.id)}
                className={`p-4 rounded-lg border cursor-pointer transition-all ${
                  selectedStrategy === strategy.id
                    ? 'bg-amber-900/30 border-amber-500/50 ring-2 ring-amber-500/30'
                    : 'bg-slate-800/30 border-slate-700 hover:border-slate-600'
                }`}
              >
                <div className="flex items-start justify-between mb-2">
                  <div>
                    <h4 className="font-bold text-amber-400">{strategy.name}</h4>
                    <p className="text-xs text-slate-400">{strategy.theme}</p>
                  </div>
                  <div className={`px-2 py-1 rounded text-xs font-mono ${
                    strategy.status === 'active' ? 'bg-green-500/20 text-green-300' : 'bg-blue-500/20 text-blue-300'
                  }`}>
                    {strategy.status}
                  </div>
                </div>
                <div className="space-y-2">
                  <p className="text-xs text-slate-300">{strategy.lastSignal}</p>
                  <div className="flex justify-between text-xs">
                    <span className="text-slate-400">Confidence: <span className="text-amber-400 font-mono">{(strategy.confidence * 100).toFixed(0)}%</span></span>
                    <span className="text-slate-400">Position: <span className="text-cyan-400 font-mono">{strategy.positionSize}</span></span>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>

      {/* Strategy Details */}
      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-8">
        {/* Virtue Alignment for Selected Strategy */}
        <div className="lg:col-span-1">
          <div className="bg-slate-800/50 border border-slate-700 rounded-lg p-6 backdrop-blur">
            <h3 className="text-amber-400 font-bold text-sm mb-4">STRATEGY VIRTUES</h3>
            <div className="space-y-2">
              {virtues.map((virtue) => {
                const weight = strategies.find(s => s.id === selectedStrategy)?.virtueWeight[virtue.id] || 0;
                return (
                  <div key={virtue.id} className="flex items-center justify-between text-xs">
                    <div>
                      <p className="text-slate-300 font-mono">{virtue.id.toUpperCase()}</p>
                      <p className="text-slate-500">{virtue.principle}</p>
                    </div>
                    <div className={`font-bold ${weight >= 0.9 ? 'text-green-400' : 'text-slate-400'}`}>
                      {(weight * 100).toFixed(0)}%
                    </div>
                  </div>
                );
              })}
            </div>
          </div>
        </div>

        {/* Trading Metrics */}
        <div className="lg:col-span-2">
          <div className="grid grid-cols-2 gap-4">
            <div className="bg-slate-800/50 border border-slate-700 rounded-lg p-4 backdrop-blur">
              <p className="text-slate-400 text-xs mb-2">Live Chains</p>
              <p className="text-2xl font-bold text-blue-400">4</p>
              <p className="text-xs text-slate-500 mt-2">Base • Monad • Fraxtal • Arb</p>
            </div>
            <div className="bg-slate-800/50 border border-slate-700 rounded-lg p-4 backdrop-blur">
              <p className="text-slate-400 text-xs mb-2">Active Pools</p>
              <p className="text-2xl font-bold text-cyan-400">12</p>
              <p className="text-xs text-slate-500 mt-2">Aerodrome • 1inch • 0x</p>
            </div>
            <div className="bg-slate-800/50 border border-slate-700 rounded-lg p-4 backdrop-blur">
              <p className="text-slate-400 text-xs mb-2">Avg Slippage</p>
              <p className="text-2xl font-bold text-green-400">0.38%</p>
              <p className="text-xs text-slate-500 mt-2">Jin virtue: optimized</p>
            </div>
            <div className="bg-slate-800/50 border border-slate-700 rounded-lg p-4 backdrop-blur">
              <p className="text-slate-400 text-xs mb-2">Win Rate</p>
              <p className="text-2xl font-bold text-amber-400">73%</p>
              <p className="text-xs text-slate-500 mt-2">Makoto principle: honest</p>
            </div>
          </div>
        </div>
      </div>

      {/* Virtue Audit Log */}
      <div className="bg-slate-800/50 border border-slate-700 rounded-lg p-6 backdrop-blur">
        <h3 className="text-amber-400 font-bold text-sm mb-4">VIRTUE AUDIT LOG</h3>
        <div className="space-y-2">
          {auditLog.map((log, idx) => (
            <div key={idx} className="flex items-center justify-between p-3 bg-slate-900/40 border border-slate-700/50 rounded text-xs">
              <div className="flex-1">
                <p className="text-slate-300 font-mono">{log.time}</p>
                <p className="text-slate-400">{log.action} • {log.asset || log.signal || log.pool}</p>
              </div>
              <div className="flex items-center gap-3">
                <span className="text-slate-400">{log.virtue.toUpperCase()}</span>
                {log.status === 'success' ? (
                  <CheckCircle className="w-4 h-4 text-green-400" />
                ) : (
                  <AlertCircle className="w-4 h-4 text-yellow-400" />
                )}
              </div>
            </div>
          ))}
        </div>
      </div>

      {/* Developer Console Footer */}
      <div className="mt-8 p-4 bg-slate-900/50 border border-slate-700 rounded-lg border-t-2 border-t-amber-500/50">
        <p className="text-xs text-slate-400 font-mono">
          <span className="text-amber-400">$</span> Ready for Base MCP integration • Signal alignment engine • Swap orchestrator active
        </p>
      </div>
    </div>
  );
};

export default BushidoTradingDashboard;